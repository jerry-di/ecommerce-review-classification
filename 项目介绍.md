# 电商评论情感与维度分类系统 —— 项目介绍

> 一个「多任务深度学习 + 大模型辅助 + 前后端分离」的电商评论智能分析系统。
> 既能对单条评论做情感（好评/差评）与评价维度（物流/质量/价格/客服/包装）分类，
> 也能对一批评论做商品维度的综合分析并打分，并配套了带 3D 渐变主题的 Web 前端、用户注册登录与历史记录。

---

## 一、项目概述

电商平台上每天产生海量商品评论。对商家而言，只知道「用户满意还是不满意」远远不够，更重要的是**搞清楚用户在哪一环节出了问题**——是物流慢、质量差、价格贵、客服态度不好，还是包装破损？

本项目解决两个递进的问题：

| 问题 | 本项目的答案 |
| --- | --- |
| 这条评论是好评还是差评？ | 情感二分类（好评 / 差评） |
| 用户主要在说哪个方面？ | 评价维度六分类（物流 / 质量 / 价格 / 客服 / 包装 / 其它） |

并且把这两件事做成一个**共享参数的深度学习模型**，一次性同时输出情感 + 维度，再辅以**大模型（DeepSeek）兜底**提升疑难样本的准确率，最终以 **Web 服务 + 可视化前端**的形式交付。

---

## 二、核心任务

### 2.1 情感二分类
- 输入：一条电商评论文本
- 输出：`好评` / `差评`

### 2.2 评价维度六分类
- 输入：一条电商评论文本
- 输出：`物流` / `质量` / `价格` / `客服` / `包装` / `其它`（取评论主要强调的那个方面）

### 2.3 批量商品分析（打分）
- 输入：十几条甚至上百条评论（文本或文件）
- 输出：商品综合评分（0~5 星）+ 各维度好评率 + 差评痛点样例

---

## 三、系统架构

采用经典的 **离线训练 + 在线推理** 分离架构，外加一层 **大模型辅助** 与 **前端展示**：

```
┌────────────────────────────────────────────────────────────────────┐
│                           数据层                                   │
│  京东评论 CSV（情感标签） + EPRSTMT 电商语料（无标签）              │
│         ↓ 清洗            ↓ DeepSeek 伪标注（单次/定向/多票表决）   │
│  情感训练集(4.5万)      维度训练集(9882) / 验证集(1745)            │
└────────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────────┐
│                          模型层（离线训练）                         │
│         共享 BERT encoder（bert-base-chinese）                     │
│            ├── 情感头（2 类）                                      │
│            └── 维度头（6 类）  ← class_weight + 早停 + 保存最优     │
└────────────────────────────────────────────────────────────────────┘
                              ↓ 保存 encoder + 双头权重
┌────────────────────────────────────────────────────────────────────┐
│                          服务层（Flask 在线推理）                   │
│  /api/v3/classify  单条/批量分类                                    │
│  /api/v3/analyze   批量商品分析打分                                 │
│  /api/auth/*       注册/登录/退出/当前用户                          │
│  /api/history      历史评论记录（SQLite）                           │
│         低置信度样本 ──→ DeepSeek 兜底重判                          │
└────────────────────────────────────────────────────────────────────┘
                              ↓ HTTP/JSON
┌────────────────────────────────────────────────────────────────────┐
│                          前端层（3D 渐变单页应用）                  │
│  单条分类 · 批量分析打分 · 登录注册 · 历史记录                      │
└────────────────────────────────────────────────────────────────────┘
```

---

## 四、技术栈

| 层次 | 技术 |
| --- | --- |
| 语言 | Python 3.12 |
| 深度学习 | PyTorch 2.8 + Transformers / ModelScope（加载 `google-bert/bert-base-chinese`） |
| 后端 | Flask（应用工厂模式 + 蓝图 + 版本化路由） |
| 存储 | 冷热隔离：Redis（热缓存）+ SQLite（结构化）+ FAISS 向量库（冷层语义检索） |
| 大模型 | DeepSeek（`deepseek-chat`，OpenAI 兼容接口，用 `requests` 直调） |
| 数据处理 | pandas / scikit-learn（清洗、分层切分、类别权重） |
| 前端 | 原生 HTML/CSS/JS（单文件、零依赖、3D 渐变 + 玻璃拟态） |

---

## 五、数据

### 5.1 情感数据（有现成标签）

来源：京东评论 CSV（`data/pre/train.csv`、`dev.csv`，`sentence,label,dataset` 三列，`label` 为 0/1）。

清洗步骤：丢弃空标签/空文本 → 去掉制表符和换行 → 只保留 `句子\t标签` 两列。

| 文件 | 条数 | 标签分布 |
| --- | --- | --- |
| `data/raw/jd_sentiment_train.txt` | 45,010 | 差评 22,588 / 好评 22,422 |
| `data/raw/jd_sentiment_valid.txt` | 4,988 | 差评 2,547 / 好评 2,441 |

### 5.2 维度数据（无现成标签 → 大模型伪标注）

来源：**EPRSTMT**（FewCLUE 小样本基准的电商评论子集），带标签的只有 320 条情感标签，没有「评价维度」标签。因此维度标签全部由 **DeepSeek 伪标注**生成，经历了四轮：

| 轮次 | 方式 | 结果 |
| --- | --- | --- |
| ① 冷启动标注 | few-shot 提示，随机抽 5000 条无标签评论 | 成功 4,913 条 |
| ② 定向补标稀有类 | 关键词筛选候选（物流/价格/客服/包装），共 4,640 条 | 稀有类从几十条补到 900+ |
| ③ 包装专项补标 | 从 5 万京东评论里筛「包装/盒子/破损」候选 2,349 条 | 包装从 299 补到 1,155 |
| ④ 多票表决清洗 | 每条投 3 票（`temperature=0.7`），取多数票 | 见下表 |

**多票表决结果**（用于降低标签噪声）：
- 三票全一致率：**76.7%**（即约 1/4 样本本身有分歧）
- 标签被多数票纠正：**7.1%**（825 条原标签是错的）
- 丢弃（三票全不同 / 断网失败）：275 条

最终维度数据 **11,627 条**，分层切分为：

| 文件 | 条数 |
| --- | --- |
| `data/raw/eprstmt_aspect_train.txt` | 9,882 |
| `data/raw/eprstmt_aspect_valid.txt` | 1,745 |

**六类分布**：物流 1,301 / 质量 5,468 / 价格 959 / 客服 1,002 / 包装 1,125 / 其它 1,772（质量类天然占近半数，属长尾分布）。

---

## 六、模型

### 6.1 结构：多任务学习（Multi-Task Learning）

```
                输入: "耳机音质很差，用了两天就坏了"
                              │
                       BERT encoder（共享）
                       bert-base-chinese
                       hidden = 768
                              │  pooler_output
              ┌───────────────┴───────────────┐
              │                               │
        情感头 Linear(768→2)           维度头 Linear(768→6)
        softmax 输出好评/差评         softmax 输出物流/质量/...
```

核心类 `MultiTaskBERT`（`src/model_train_mtl.py`）：
- 共享 BERT encoder：两个任务共用同一个语义表征层；
- 两个独立分类头：情感头（2 类）、维度头（6 类），互不干扰。

**为什么多任务有效**：5 万条情感数据帮 encoder 学到更强的电商领域语义表征，间接提升只有 1 万条的维度分类；两者标签相关（「音质差」= 负面+质量），共享能互相促进。

### 6.2 关键训练技巧

| 技巧 | 说明 |
| --- | --- |
| 类别不平衡 | 维度头用 `class_weight`（反频率权重）抑制质量类独大 |
| 早停 | 维度验证准确率连续 2 个 epoch 不提升即停，防止过拟合 |
| 只保存最优 | 仅在验证准确率创新高时保存 checkpoint，避免过拟合模型覆盖最优模型 |
| 断点续训 | `--resume` 从上次 checkpoint 加载 encoder + 双头继续训 |
| 预分词优化 | 训练前一次性批量分词整个语料，避免 `num_workers=0` 时每步逐条切词的瓶颈（训练速度提升约 5 倍） |
| 双任务交替 | `itertools.cycle` 让较小的维度 loader 循环，与情感 loader 同步步进 |

### 6.3 训练配置

| 参数 | 值 |
| --- | --- |
| 预训练模型 | `google-bert/bert-base-chinese`（1.1 亿参数） |
| 优化器 | AdamW，`lr = 2e-5` |
| 损失 | `loss = loss_情感 + λ·loss_维度`，`λ = 1.0` |
| batch | 情感 256 / 维度 64 |
| max_len | 32 |
| 设备 | CUDA（NVIDIA RTX 4060 Laptop，8GB） |
| 早停 | `patience = 2`，最多 8 epoch |

### 6.4 性能结果

| 任务 | 准确率 | 说明 |
| --- | --- | --- |
| 情感二分类 | **90.24%** | 真实标签，5 万条训练 |
| 维度六分类 | **72.49%** | 与 DeepSeek 标签的吻合度（最优 epoch 保存） |

> 维度 72.49% 的含义：这是「BERT 与 DeepSeek 标签的一致率」，不是人工正确率。
> 其天花板受限于 DeepSeek 自身的标注自洽度（多票表决测得仅 76.7%）。
> 6 类中「客服 vs 质量」等边界本身就模糊，人类标注一致性通常也只有 70~80%。

---

## 七、推理：本地模型 + 大模型兜底（混合推理）

纯本地 BERT 推理很快（实测单条约 5.7ms，GPU），但疑难样本准确率受限。因此设计了**低置信度兜底**策略：

```
本地模型预测（情感 + 维度 + 置信度）
        │
        ├─ 维度置信度 ≥ 0.8 → 直接用本地结果（快、免费）
        │
        └─ 维度置信度 < 0.8 → 调 DeepSeek 重判维度（更准）
                                └─ DeepSeek 失败 → 降级回本地结果
```

- 95% 流量走本地模型（毫秒级、免费），只有低置信度的少数样本走 DeepSeek（约 1~3 秒）；
- 兜底阈值可配置（默认 0.8），DeepSeek 挂掉自动降级，不影响可用性。

命令行推理入口：`python -m src.predict_mtl "评论" [--threshold 0.8] [--no-fallback]`。

---

## 八、后端（Flask）

采用**应用工厂模式**（`app/__init__.py` 的 `create_app()`），按蓝图组织路由。

### 8.1 接口清单

| 方法 | 路径 | 功能 | 鉴权 |
| --- | --- | --- | --- |
| POST | `/api/v3/classify` | 单条/批量分类（含兜底） | 否 |
| POST | `/api/v3/analyze` | 批量商品维度分析 + 打分 | 否 |
| POST | `/api/v3/similar` | 语义检索相似评论（冷层 FAISS） | 否 |
| POST | `/api/auth/register` | 注册 | 否 |
| POST | `/api/auth/login` | 登录（写 session） | 否 |
| POST | `/api/auth/logout` | 退出 | 否 |
| GET | `/api/auth/me` | 当前登录用户 | 否 |
| POST | `/api/history` | 保存历史评论 | 是 |
| GET | `/api/history` | 查询我的历史评论 | 是 |
| POST | `/api/async/classify` | 异步分类（返回 task_id） | 否 |
| POST | `/api/async/analyze` | 异步批量分析 | 否 |
| GET | `/api/async/result/<task_id>` | 查询异步任务结果 | 否 |
| GET | `/api/async/stats` | 异步队列统计 | 否 |

### 8.2 数据存储（SQLite）

- 文件：`tmf.db`（项目根目录，已加入 `.gitignore`）
- `users` 表：用户名 + 密码哈希（Werkzeug `scrypt`，**不存明文**）+ 注册时间
- `history` 表：评论文本 + 情感 + 维度 + 置信度 + 是否兜底 + 时间，通过 `user_id` 关联用户

### 8.3 模型加载

`app/extensions.py` 的 `TextClassifierExtension` 单例：
- 延迟加载多任务模型（encoder + 双头权重）；
- `warmup()` 在应用启动时预热，避免首个请求卡顿；
- `predict()` 封装「本地推理 + 低置信度 DeepSeek 兜底」。

### 8.4 冷热隔离存储（Redis + SQLite + FAISS）

评论数据按「冷热」分层存储，各司其职：

| 层 | 存储 | 存什么 | 特征 |
| --- | --- | --- | --- |
| 🔥 热层 | Redis | 最近分类过的评论结果（TTL 1 小时自动淘汰） | 内存、命中即秒回、免重复计算 |
| 🌡️ 温层 | SQLite | 用户账号 + 历史评论记录（结构化） | 持久、可靠、按 `user_id` 关联 |
| ❄️ 冷层 | FAISS 向量库 | 全量评论的 768 维语义向量（落盘持久化） | 海量、按语义做相似评论检索 |

**数据流转**：

```
评论请求 → 查 Redis 热缓存 → 命中？→ 直接返回（秒回）
                    ↓ miss
              模型推理（BERT + DeepSeek 兜底）
                    ↓ 写回
        ┌──────────┼─────────────┐
        ↓          ↓              ↓
     Redis       SQLite        FAISS
    （热缓存）   （历史记录）   （语义向量，落盘）
```

**冷层语义检索**（`POST /api/v3/similar`）：输入一条评论，用共享 BERT 计算其向量，在 FAISS 里按余弦相似度找语义最相似的评论（例如查「快递太慢了」能命中「物流很快」这类同为物流话题的评论）。

> 说明：Milvus 在 Windows 下需额外内嵌包且兼容性差，故冷层选用 FAISS（Milvus 内部实际也是用 FAISS 做向量索引），效果等价、更轻量。

### 8.5 异步任务处理（防高并发）

为应对多访问并发「扛不住」的问题，提供异步任务接口：

| 机制 | 说明 |
| --- | --- |
| 异步入队 | 请求入队后**立刻返回 task_id**，Web 请求不再阻塞在模型推理上 |
| 受限线程池 | 后台 `ThreadPoolExecutor`（2 个工作线程）执行任务 |
| GPU 串行化 | 信号量保证同时只有一个任务跑 GPU 推理，保护显存不被并发打爆 |
| 队列上限 | 队列超过 50 个任务返回 429，防止无限堆积 |
| 轮询取结果 | 客户端轮询 `GET /api/async/result/<task_id>` 获取结果 |

**异步接口**：

| 方法 | 路径 | 功能 |
| --- | --- | --- |
| POST | `/api/async/classify` | 提交异步分类任务，返回 task_id |
| POST | `/api/async/analyze` | 提交异步批量分析任务 |
| GET | `/api/async/result/<task_id>` | 查询任务结果（pending/running/done/error） |
| GET | `/api/async/stats` | 队列统计（运行中/排队/完成数） |

**线程安全**：模型推理用 `_infer_lock` 串行化，FAISS 向量库用自身锁串行化写入，避免并发下模型/索引被破坏。

---

## 九、前端

`front/index.html` —— 单文件、零依赖的 SPA。

### 9.1 视觉设计（3D 渐变主题）
- **3D 渐变背景**：4 个动态彩色光斑（紫/粉/蓝）+ 3D 透视网格 + 上浮粒子；
- **玻璃拟态卡片**：毛玻璃模糊 + 渐变描边，鼠标移动时 3D 倾斜。

### 9.2 功能模块
- **单条评论分类**（左栏）：输入一条评论 → 显示情感/维度/置信度/是否兜底；
- **批量商品分析**（右栏）：粘贴多条评论或上传 txt 文件 → 综合评分（星级）+ 各维度好评率条 + 差评痛点；
- **登录/注册**：弹窗表单，登录后顶栏显示用户名；
- **历史记录**：登录后分类自动保存，历史面板展示过往评论。

---

## 十、目录结构

```
tmf_v1/
├── wsgi.py                 # Web 启动入口（首页返回前端页面）
├── 项目介绍.md              # 本文档
├── tmf.db                  # SQLite 数据库（gitignore）
├── src/                    # —— 离线模型侧 ——
│   ├── config.py           # 配置（路径、num_labels、超参，dataclass）
│   ├── model_train_mtl.py  # 多任务训练（早停 + 保存最优 + 续训）
│   ├── predict_mtl.py      # 命令行推理（含 DeepSeek 兜底）
│   ├── pseudo_label.py     # DeepSeek 伪标注（few-shot）
│   ├── pseudo_label_boost.py  # 关键词定向补标稀有类
│   ├── pseudo_label_pack.py   # 包装专项补标
│   ├── pseudo_label_vote.py   # 多票表决清洗标签
│   └── utils.py            # 通用工具（get_corpus）
├── app/                    # —— 在线服务侧 ——
│   ├── __init__.py         # 应用工厂 create_app()
│   ├── config.py           # Web 配置（环境映射、SECRET_KEY）
│   ├── extensions.py       # 多任务模型加载 + 混合推理
│   ├── db.py               # SQLite 访问层
│   ├── storage.py          # 冷热隔离存储层（Redis 热缓存 + FAISS 冷向量库）
│   ├── auth.py             # 注册/登录/退出蓝图
│   ├── history.py          # 历史记录蓝图
│   ├── analyze.py          # 批量商品分析 + 相似检索蓝图
│   ├── async_tasks.py      # 异步任务处理（线程池 + GPU 信号量限流）
│   └── v3/prediction.py    # 分类接口蓝图
├── front/
│   └── index.html          # 前端（单文件 SPA）
├── data/                   # 数据
│   ├── raw/                # 清洗后的训练/验证语料
│   ├── pre/                # 原始京东评论 CSV
│   └── eprstmt_class.txt   # 六类维度标签
└── model/                  # 训练产物（gitignore）
    ├── ft_bert_model/      # 共享 BERT encoder + tokenizer
    └── ft_bert_model_mtl_heads.pt  # 双头权重
```

---

## 十一、运行方式

### 11.1 启动服务
```bash
cd tmf_v1
python wsgi.py          # 默认 http://127.0.0.1:8000
```

### 11.2 重新训练 / 续训
```bash
python -m src.model_train_mtl --epochs 8 --patience 2          # 从头训
python -m src.model_train_mtl --resume --epochs 5 --patience 2 # 续训
```

### 11.3 命令行推理
```bash
python -m src.predict_mtl "物流很快第二天就到了"
```

### 11.4 接口调用示例
```bash
curl -X POST http://127.0.0.1:8000/api/v3/classify \
  -H "Content-Type: application/json" \
  -d '{"text":"耳机音质很差"}'
```

---

## 十二、技术亮点总结

1. **多任务学习**：共享 BERT + 双头，用海量情感数据反哺小样本的维度分类；
2. **大模型数据闭环**：DeepSeek 伪标注（冷启动 → 定向补标 → 多票表决清洗）解决无标签数据的冷启动问题；
3. **大模型推理兜底**：本地模型打主力、DeepSeek 兜底疑难样本，兼顾成本与准确率；
4. **工程化训练**：早停、保存最优、断点续训、预分词加速，避免过拟合与算力浪费；
5. **前后端分离**：Flask 提供 REST API，3D 渐变前端直连接口，附带用户体系与历史记录。

---

*作者：电商评论情感与维度分类系统 · 2026*
